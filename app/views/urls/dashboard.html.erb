<div class="py-16 sm:py-24">
  <div class="mb-16 sm:mb-32">
    <%= render 'urls/header' %>
  </div>
  <div class="px-8 sm:py-8 sm:px-16 bg-white ring-1 ring-gray-900/5 rounded-2xl shadow-lg divide-y divide-gray-500/10">
    <div class="flex flex-col border-gray-100 p-6 text-center">
      <dt class="order-2 mt-2 text-lg font-medium leading-6 text-gray-500">Clicks</dt>
      <dd class="order-1 text-5xl font-bold tracking-tight text-orange-400" id="total_clicks"><%= @url.click %></dd>
    </div>
    <div class="space-y-2 py-8">
      <h3 class="text-2xl font-bold tracking-tight text-gray-900">Total URL Clicks</h3>
      <p>The total number of clicks that your link has received so far.</p>
    </div>
    <div class="space-y-2 py-8">
      <p class="text-md">Original URL: <%= link_to "#{@url.long_url}", @url.long_url, target: "_blank", class: "leading-7 text-orange-400 hover:text-orange-500" %></p>
      <p class="text-md">Your shortened URL: <%= link_to "#{@url.short_url}", url_redirect_path(@url), target: "_blank", class: "leading-7 text-orange-400 hover:text-orange-500" %></p>
    </div>
    <div class="space-y-2 py-8">
      <p>Create another <%= link_to "shortened URL", root_path, target: "_blank",class: "text-orange-400 hover:text-orange-500" %>.</p>
    </div>
  </div>
</div>